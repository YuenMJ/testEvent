Q1 :
SELECT COUNT(*) FROM 
	SELECT TRANSACTION.ID from USER
		LEFT JOIN ACCOUNT ON USER.ID = ACCOUT.USER_ID
		LEFT JOIN TRANSACTION ON ACCOUNT.ID = TRANSACTION.ACCOUNT_ID
		INNER JOIN CATEGORY ON CATEGORY.ID = TRANSACTION.CATEGORY_ID
	WHERE USER.ID = '356789' AND CATEGORY.NAME = '15';

Q2 :
SELECT TRANSACTION.ID, TRANSACTION.ACCOUNT_ID, TRANSACTION.CATEGORY_ID, TRANSACTION.NAME, TRANSACTION.AMOUNT, TRANSACTION.POSTED_DATE FROM TRANSACTION
	INNER JOIN ACCOUNT ON TRANSACTION.ACCOUNT_ID = ACCOUNT.ID AND ACCOUNT.ID = 'account number' AND TRANSACTION.POSTED_DATE LIKE 'YYYYMM%';

Q3 :
SELECT CATEGORY.ID, CATEGORY.NAME FROM CATEGORY
	LEFT JOIN TRANSACTION ON CATEGORY.ID = TRANSACTION.CATEGORY_ID AND TRANSACTION.AMOUNT > 0
	LEFT JOIN ACCOUNT ON TRANSACTION.ACCOUNT_ID = ACCOUNT.ID
	INNER JOIN USER ON ACCOUNT.USER_ID = USER.ID AND USER.NAME = 'user wanted';